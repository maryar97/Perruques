<form method="post" action="{{ path('app_produit_crud_supprimer', {'id': produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('supprimer' ~ produit.id) }}">
    <button class="btn">Supprimer</button>
</form>
